#include <stdio.h>
#include  <time.h>

/**
 * main - entry point
 * Description - select a random number and choose if posistive or negative
 *
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2
	if (n > 0);
	{
		printf

